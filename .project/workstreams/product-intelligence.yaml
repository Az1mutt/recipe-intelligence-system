schema_version: "0.2"

state_scope: "workstream"

project:
  name: "Recipe Intelligence System"
  workstream: "Product & Intelligence"
  owner: "unassigned"

status: "future_unassigned"
phase: "not_active"

current_gate: >
  None. This workstream should only be activated when a real specialist owner
  exists for product behavior, agent roles, recommendation/learning logic,
  review/promotion semantics, preferences, or human-in-the-loop UX.

current_state: >
  A future Product & Intelligence boundary is reserved so product/agent behavior
  does not become entangled with technical platform implementation. There is no
  dedicated specialist chat for this workstream today. The manual recipes chat
  is not treated as its owner.

exact_next_action: >
  Do nothing. Activate and initialize this workstream only when a dedicated
  Product & Intelligence owner or sustained agent/product-design work actually exists.

blockers: []

dependencies:
  - project: "Recipe Intelligence System"
    workstream: "Data & Platform Architecture"
    dependency: >
      If activated later, this workstream should consume implemented platform
      capabilities and express product requirements without duplicating schema/
      ingestion implementation details.

important_open_loops:
  - "Future: define agent roles and product behavior when there is a real need."
  - "Future: define recommendation/learning/review semantics if they become a sustained separate workstream."

portfolio:
  milestone: false
  signal: ""

verification:
  last_verified: "2026-09-09"
  sources:
    - "Project OS clarification: no dedicated Product & Intelligence specialist chat currently exists."

sync:
  status: "synced"
  storage: ".project/workstreams/product-intelligence.yaml"
